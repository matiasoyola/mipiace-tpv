node_modules/
dist/
dev-dist/
build/
.next/
.turbo/
.cache/

.env
.env.*
!.env.example
!**/.env.example
!**/.env.*.example
**/.env

.claude/

*.log
logs/

# Informes Z generados en runtime (y por los tests, que instancian el
# generador de PDF de verdad). No son fuente.
storage/

.DS_Store
.idea/
.vscode/

coverage/
*.tsbuildinfo

# Volcados del MCP de Playwright (snapshots y logs de consola del
# bucle visual). Las capturas que sí se guardan viven en docs/blocks/.
.playwright-mcp/

spike/**/fixtures/*
!spike/**/fixtures/.gitkeep

packages/db/node_modules/.prisma/

# Mirror local del repo legacy (matiasoyola/mipiace-tpv). Lo tenemos en
# local como referencia. El histórico vive en GitHub; en nuestro repo
# capturamos los hallazgos en docs/00-analisis-legacy.md.
legacy/
docs/manuales/

# A4 · salidas temporales de infra/test/bundle-android.test.ts (dos vite build
# a un outDir aparte para no pisar el dist de trabajo).
apps/tpv-web/.a4-test-dist-*/
